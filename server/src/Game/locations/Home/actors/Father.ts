import { Actor } from "../../../../Game/components/Actor";

export const father = new Actor({
    name: 'Andrei',
    type: 'NPC',
    texture: 'fat',
    portrait: 'fat',
})

father.x = 500
father.y = 500
